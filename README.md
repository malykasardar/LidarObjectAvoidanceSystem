Required dependencies installed:
   * ROS (Robot Operating System): Specifically the rplidar_ros package for the
     LiDAR and standard message types like sensor_msgs and geometry_msgs.
     * Python 3: For both the safety governor and the webcam viewer.
